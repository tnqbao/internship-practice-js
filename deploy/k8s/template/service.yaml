apiVersion: v1
kind: Service
metadata:
  name: practice-js-service
  namespace: agility-${DEPLOY_ENV}-env
spec:
  selector:
    app: practice-js-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---